function qValue = update(Q, state, state2, reward, action, action2, alpha, gamma)    predict = Q(state, action);  target = reward + gamma*Q(state2, action2)-predict;  #Q(state, action) = Q(state, action) + alpha * (target - predict);      Q(state, action) = predict + alpha*(target);  qValue =  Q(state, action);  end