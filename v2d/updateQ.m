function qValue = update(Q, state, state2, reward, action, alpha, gamma)      predict = Q(state, action);  target = reward + gamma*Q(state2, action);  Q(state, action) = Q(state, action) + alpha * (target - predict);  qValue = Q(state, action);  end